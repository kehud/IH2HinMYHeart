import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ih2hpom.app',
  appName: 'ih2hpom',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
